import { test } from '@playwright/test';

test.describe('customer session flow', () => {
  test.fixme('new customer order creates secure session and returning visit is prefilled');
  test.fixme('customer logout removes session and hides greeting');
});
